_schema-version: "3.2"
ID: travustmatravelproc
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travustmatravelproc-destination-content
  type: com.sap.application.content
  requires:
  - name: travustmatravelproc-destination-service
    parameters:
      content-target: true
  - name: travustmatravelproc-repo-host
    parameters:
      service-key:
        name: travustmatravelproc-repo-host-key
  - name: travustmatravelproc-uaa
    parameters:
      service-key:
        name: travustmatravelproc-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travustmatravelproc_html_repo_host
          ServiceInstanceName: travustmatravelproc-html5-srv
          ServiceKeyName: travustmatravelproc-repo-host-key
          sap.cloud.service: travustmatravelproc
        - Authentication: OAuth2UserTokenExchange
          Name: travustmatravelproc_uaa
          ServiceInstanceName: travustmatravelproc-xsuaa-srv
          ServiceKeyName: travustmatravelproc-uaa-key
          sap.cloud.service: travustmatravelproc
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travustmatravelproc-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travustmatravelproc-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travustmatravelproc.zip
      name: travustmatravelproc
      target-path: resources/
- name: travustmatravelproc
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travustmatravelproc-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travustmatravelproc-destination-service
    service-plan: lite
- name: travustmatravelproc-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travustmatravelproc-xsuaa-srv
    service-plan: application
- name: travustmatravelproc-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travustmatravelproc-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
